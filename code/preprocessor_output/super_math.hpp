#ifndef PREPROCESSOR_OUTPUT_SUPER_MATH_HPP
#define PREPROCESSOR_OUTPUT_SUPER_MATH_HPP

#define SQRT(x) ((x) * (x))
#define MIN(a, b) (( (a) < (b)) ? (a) : (b))

int add_two_numbers(int first, int second);

#endif //PREPROCESSOR_OUTPUT_SUPER_MATH_HPP
